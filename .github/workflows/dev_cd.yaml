name : dev-cd-wf
on:
  push:
    branches:
      - develop
jobs:
  develop_deploy_job:
    runs-on: ubuntu-latest
    environment: "development-ev"
    steps:
      - name: deploydevelop branch code on render
        run: curl -X POST ${{secrets.RENDER_API_KEY}}


